[project]
name = "clipforge"
version = "0.1.0"
description = "Automated clip generation pipeline"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "python-dotenv"
]

[project.scripts]
clipforge = "clipforge.render_clip:main"

[project.optional-dependencies]
dev = [
    "pytest"
]
